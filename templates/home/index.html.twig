{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Bienvenue</h1>
    <div class="home-btn">
        <p>Besoin de transformer vos fichiers en données plus fiables?</p>
        <p>Nous sommes là pour vous</p>
        <div class="row justify-content-center">
        <div class=""col-12 col-md-8 bg-blue p-4 m-0 text-primary>
            {{ form_start(form) }}
            {{ form_widget(form.filetype) }}
            {{ form_end(form) }}
        </div>
        </div>
        {# <a href="{{ path('app_data') }}" class="btn-btn secondary"> Cliquez-ici</a> #}
    </div>
</div>
{% endblock %}
